import uglify from 'rollup-plugin-uglify';
import config from './rollup.config.js';

config.plugins.push(uglify());

config.format = 'umd';
config.dest = 'dist/cornerstone.min.js';

export default config;
